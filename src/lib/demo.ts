/**
 * Public demo account credentials. Intentionally not secret — these power the
 * "Try the demo" buttons so visitors can explore without signing up.
 * Re-run `npm run seed:demo` to reset the demo data.
 */
export const DEMO_COACH_EMAIL = "demo@reppod.app";
export const DEMO_CLIENT_EMAIL = "demo.client@reppod.app";
export const DEMO_PASSWORD = "demoflex123";
